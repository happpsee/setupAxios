# 代码审查报告

## 🔴 严重 Bug（功能直接失效）

---

### Bug 1 · `commonRequest.ts` — `isTempory` 逻辑完全反了

**位置：** `addReqInterceptor` / `addResInterceptor` 函数

```ts
// 当前代码（有 Bug）
const addReqInterceptor = (fn: ReqInterceptor, isTempory = false) => {
  if (!isTempory) {       // ← 错！永久拦截器被加进了 temporySet
    temporySet.add(fn);
  }
  reqInterceptor.push(fn);
};
```

`temporySet` 的作用是标记"用完就删"的临时拦截器。但现在 `isTempory = false`（永久）时，`!isTempory` 为 `true`，反而把它加进了 `temporySet`。
结果：**所有"永久"拦截器在请求一次后被清除，"临时"拦截器反而永远留着**，行为与设计完全相反。

**修复：**
```ts
const addReqInterceptor = (fn: ReqInterceptor, isTempory = false) => {
  if (isTempory) {   // ← 临时的才加进 temporySet
    temporySet.add(fn);
  }
  reqInterceptor.push(fn);
};
```

`addResInterceptor` 存在完全相同的问题，同样需要修复。

---

### Bug 2 · `utils/urlParse.ts` — `new URL()` 参数顺序反了

**位置：** `urlParamsParse` 函数

```ts
// 当前代码（有 Bug）
const url = new URL(config.baseUrl, config.url);
//                  ↑ 这是相对路径   ↑ 这是 base
```

`new URL(url, base)` 的语义是"把第一个参数当相对路径，拼在第二个参数 base 上"。
现在把 `baseUrl` 放第一位，若 `baseUrl` 是绝对路径（如 `https://api.example.com`），`base` 参数会被完全忽略，导致 **`config.url` 里的路径（如 `/users/1`）全部丢失**，请求只会发到 `baseUrl` 本身。

**修复：**
```ts
const url = new URL(config.url, config.baseUrl);
//                  ↑ 路径        ↑ base
```

---

### Bug 3 · `tools/log.ts` + `tools/index.ts` — Log 工厂函数从未被调用，日志永远不打印

**位置：** `tools/log.ts` 导出的是工厂函数，`tools/index.ts` 把工厂本身存进了 `toolMap`

```ts
// log.ts —— Log 是一个工厂函数，调用后才返回真正的工具函数
export const Log = (): ToolInstanceType => {
  return function (cfg) { /* 实际的 log 逻辑 */ };
};

// index.ts —— 把工厂函数本身存了进去（没有调用）
const toolMap = new Map([["log", Log]]);

// 执行时
for (const item of toolMap.values()) {
  await Promise.resolve((item as any)(decoratorInstance));
  // 实际执行 Log(decoratorInstance)
  // → Log 忽略参数，只是返回内部函数，内部函数被 Promise.resolve 包裹后直接丢弃
  // → 日志从来不会打印
}
```

**修复方案一：** 存工厂的执行结果
```ts
// index.ts
const toolMap = new Map([["log", Log()]]);  // ← 调用一次，存返回值
```

**修复方案二：** 把 Log 改成工具函数而非工厂
```ts
// log.ts
export const Log: ToolInstanceType = (cfg) => {
  console.log(`[Request Start] ===>>>`, cfg.arguments);
  cfg.registryAfter(() => {
    console.log(`[Request End] ===>>>`, cfg.result);
  });
};
```

---

### Bug 4 · `apiRequest.ts` / `jsonRequest.ts` — `addTool` 调用时缺少 `toolName` 参数

**位置：** `addDecorateTool` / `addCombinateTool` 函数

```ts
// tools/index.ts —— addTool 需要两个参数
const addTool = (toolName: string, tool: any) => {
  toolMap.set(toolName, tool);
};

// apiRequest.ts —— 只传了一个参数
export const addDecorateTool = (fn: any) => {
  addTool && addTool(fn);   // ← toolName = fn（一个函数），tool = undefined
};
```

调用 `addDecorateTool(myTool)` 实际上执行 `toolMap.set(myTool, undefined)`，**工具根本没有被加进去**。

**修复：**
```ts
export const addDecorateTool = (toolName: string, fn: any) => {
  addTool && addTool(toolName, fn);
};
```

`addCombinateTool` / `removeDecorateTool` / `removeCombinateTool` 存在同样的问题。

---

### Bug 5 · `jsonRequest.ts` — `CombinateManage` 每次调用 `useApi` 都重新创建，外部添加的工具全部失效

**位置：** `useApi` 函数内部

```ts
const useApi = async <Res, Req>(name: string, data: Req): Promise<Res> => {
  // ...
  return CombinateManage(commonRequest)(reqCfg) as Promise<Res>;
  //     ↑ 每次调用都重新执行 CombinateManage，重新创建 toolManageFactory()
  //     → 新的 toolMap 只有默认的 Log，用户通过 addCombinateTool 加的工具全部丢失
  //     → 同时每次都会覆写模块级的 addTool / removeTool 变量，导致状态混乱
};
```

**修复：** `CombinateManage` 只初始化一次，缓存包装后的函数

```ts
// 在 setupJsonAxios 内部，useApi 外部初始化
const wrappedRequest = CombinateManage(commonRequest);

const useApi = async <Res, Req>(name: string, data: Req): Promise<Res> => {
  const cfg = jsonAxiosConfig.get(name);
  if (!cfg) throw Error("没有配置当前表");
  const reqCfg = { ...cfg.target, data };
  return wrappedRequest(reqCfg) as Promise<Res>;
};
```

---

### Bug 6 · `platform/web.ts` — 没有将 `method` 写入 `requestInit`，POST/PUT/DELETE 全变成 GET

**位置：** `platformRequest` 内的 `request` 函数

```ts
const reqInit = {} as Record<string, any>;

if (cfg.method !== "GET" && cfg.data) {
  writeBodyData(reqInit, cfg);   // 只写了 body，从未设置 method
}

const requestConfig = new Request(url, reqInit);
// → fetch 默认方法是 GET，就算 body 写进去了，请求也以 GET 发出
// → 服务器收不到 body
```

**修复：**
```ts
const reqInit: RequestInit = {
  method: cfg.method,   // ← 必须加这行
};

if (cfg.method !== "GET" && cfg.data) {
  writeBodyData(reqInit, cfg);
}
```

---

## 🟡 设计问题

---

### 问题 7 · `tools/timeout.ts` — 超时实现无法真正让调用方感知异常，且无法取消已发出的请求

```ts
setTimeout(() => {
  if (config.result && !config.cancel) return;
  config.result = Promise.reject(Error("请求超时!"));  // ← 不会抛出
  config.cancel = true;
}, +timer);
```

两个问题：
1. `result = Promise.reject(...)` 只是把一个 rejected Promise 对象赋值给 `result`，装饰器最终 `return decoratorInstance.result` 会把这个对象作为**正常值**返回，调用方不会收到异常。
2. `cancel = true` 只能阻止"还没发出"的请求，对正在飞行中的 `fetch` 没有任何效果。真正的取消需要 `AbortController`。

**建议修复思路：**
```ts
export const timeoutTool = (ms: number): ToolInstanceType => {
  return (config) => {
    const controller = new AbortController();
    // 把 signal 注入请求参数，让 fetch 能感知到取消信号
    if (config.arguments[0]) {
      config.arguments[0].signal = controller.signal;
    }
    const timer = setTimeout(() => controller.abort(), ms);
    config.registryAfter(() => clearTimeout(timer));  // 请求成功后清除定时器
  };
};
```

---

### 问题 8 · `platform/web.ts` — 局部配置被全局配置覆盖（合并顺序反了）

```ts
// 当前代码
const cfg = ({ ...reqCfg, ...config }) as AllReqConfig<Req>;
//            ↑ 局部先铺   ↑ 全局后覆盖  → 全局配置优先级更高
```

通常的设计惯例是**局部配置优先级高于全局配置**（例如某个请求需要单独设置更短的超时时间）。应该反过来：

```ts
const cfg = ({ ...config, ...reqCfg }) as AllReqConfig<Req>;
//            ↑ 全局打底   ↑ 局部覆盖  → 局部配置优先级更高
```

---

### 问题 9 · `apiRequest.ts` / `jsonRequest.ts` — 模块级全局变量在多实例场景下互相覆盖

```ts
// apiRequest.ts 模块级
let addTool: any = null;
let removeTool: any = null;
```

如果用户创建了两个 `setupApiAxios` 实例，`DecorateManage` 装饰器每次执行都会用新的 `toolManageFactory()` 覆写这两个变量。
结果：`addDecorateTool` 只能影响**最后一个**创建的实例，先创建的实例无法管理工具，且行为难以预测。

---

## 🟢 小问题

| 位置 | 问题 |
|------|------|
| `commonRequest.ts` | 函数内部 `const config = reqInterceptor.reduce(...)` 遮蔽了外层参数 `config: DftBaseCfg`，可读性差，建议改名为 `interceptedConfig` 或 `reqConfig` |
| `tools/errReport.ts` | `<T extends any>` 泛型完全没有被使用，可直接删除 |
| `platform/index.ts` | `registerAdaptor` 函数没有从 `index.ts` 导出，用户无法扩展自定义适配器 |
| 整体 | `isTempory` 是 "temporary" 的拼写错误，建议统一改为 `isTemporary` 或 `temporary` |

---

## 总结

整体架构分层思路清晰，裸请求（`commonRequest`）+ 拦截器 + 装饰器工具（`toolManageFactory`）的设计是合理的。

问题主要集中在以下几类：

| 类型 | 涉及 Bug |
|------|---------|
| 逻辑条件写反 | Bug 1（`isTempory` 判断反了） |
| 参数顺序/数量错误 | Bug 2（`new URL` 顺序反了）、Bug 4（`addTool` 缺参数） |
| 工厂函数未调用就存入 | Bug 3（`Log` 没有被 `()` 调用） |
| 对象生命周期管理错误 | Bug 5（`CombinateManage` 每次请求都重建） |
| 遗漏必要字段 | Bug 6（`method` 没写进 `requestInit`） |

这些都属于"能通过类型检查但运行结果不符合预期"的问题，需要结合运行时行为才能发现。
